# WarrentyWiseTest

Monorepo with two packages:

- `components` — a small TypeScript React component library built with `tsup`.
- `store` — a Vite React + TypeScript app that consumes `@local/components`.

Quick start:

```bash
npm install
npm run dev
```

To build the library and app:

```bash
npm run build
```
